import bezier from 'bezier-easing';
/**
 * Returns the percent (0 - 100) moved based on position in the viewport
 */
export declare function getProgressAmount(start: number, totalDist: number, currentScroll: number, easing?: bezier.EasingFunction): number;
